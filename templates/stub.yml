---
meta:
  token: TOKEN
  environment: NAME
  dns_root: microbosh
  security_groups:
    - buildkite_agent
  stemcell:
    name: STEMCELL
  instances: INSTANCES

director_uuid: PLACEHOLDER-DIRECTOR-UUID

releases:
  - name: buildkite-agent
    version: latest
